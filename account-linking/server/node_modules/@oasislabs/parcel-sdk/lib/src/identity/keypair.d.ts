/**
 * An ed25519 key pair.
 */
export declare class KeyPair {
    publicKey: Uint8Array;
    private secretKey;
    static PUBLIC_KEY_LEN: number;
    static SECRET_KEY_LEN: number;
    constructor(publicKey: Uint8Array, secretKey: Uint8Array);
    static generate(): KeyPair;
    /**
     * @returns the (ed25519) signature of this key over `message`.
     */
    sign(message: Uint8Array): Uint8Array;
    toJSON(): {
        publicKey: string;
        secretKey: string;
    };
    static fromJSON(key: string, value: string): string | Uint8Array;
}
