import { Readable } from 'readable-stream';
import { TokenProvider } from '../token';
import { PutResult, Storable, Storage } from '.';
export declare const PATH_PREFIX = "parcel://";
export declare class ParcelStorage implements Storage {
    private tokenProvider;
    private apiEndpoint;
    /** `gatewayUrl` is the Parcel Gateway url stored in `Config`. */
    constructor(gatewayUrl: string, tokenProvider: TokenProvider);
    put(data: Storable): Promise<PutResult>;
    get(path: string): Readable;
    private downloadNode;
    private downloadBrowser;
    sizeOf(path: string): Promise<bigint>;
    /**
     * @returns the endpoint at which the object at `path` can be reached, if the path is valid.
     * @throws an error if the path is not valid.
     */
    private getObjectEndpoint;
    private getHeaders;
}
